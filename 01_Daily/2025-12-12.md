---
date: 2025-12-12
week: 2025-W50
tags: [daily-note]
---
# 🗓️ 2025년 12월 12일 (금요일)

**주차:** [[2025-W50]] | **작성시각:** 18:55

---
## 📅 이번 주 남은 일정
```dataviewjs
const gcal = app.plugins.plugins["google-calendar"];

if (!gcal || !gcal.api) {
    dv.paragraph("❌ Google Calendar를 불러올 수 없습니다.");
} else {
    const today = moment().startOf('day');
    const endOfWeek = moment().endOf('week');
    
    const events = await gcal.api.getEvents(today.toDate(), endOfWeek.toDate());
    
    if (!events || events.length === 0) {
        dv.paragraph("🎉 이번 주 남은 일정이 없습니다.");
    } else {
        events.sort((a, b) => {
            const aTime = a.start?.dateTime || a.start?.date;
            const bTime = b.start?.dateTime || b.start?.date;
            return new Date(aTime) - new Date(bTime);
        });
        
        let currentDate = "";
        let output = "";
        
        for (const e of events) {
            const eventStart = e.start?.dateTime || e.start?.date;
            const eventDate = moment(eventStart).format("MM/DD (ddd)");
            
            if (eventDate !== currentDate) {
                if (currentDate !== "") output += "\n";
                output += `**${eventDate}**\n`;
                currentDate = eventDate;
            }
            
            if (e.start?.date) {
                output += `- 🗓️ ${e.summary}\n`;
            } else if (e.start?.dateTime) {
                const start = moment(e.start.dateTime).format("HH:mm");
                const end = moment(e.end.dateTime).format("HH:mm");
                output += `- ⏰ ${start} ~ ${end} ${e.summary}\n`;
            }
        }
        
        dv.paragraph(output);
    }
}
```

> 💡 **일정 추가:** Ctrl + P → "Google Calendar: Create gCal Event Inside Popup"  
> 💡 **새로고침:** 일정 추가 후 Ctrl + R (노트 삭제 불필요)

---
## ✅ 오늘의 할 일

### 🆕 새 작업 추가
- [ ] 전자주민증 발급📅 2025-12-15 ⏫ ⏳ 📅 2025-12-15
- [ ] 증권회사 상담 ⏫ 📅 2025-12-16

### 🔥 오늘의 미완료 작업
```tasks
not done
path includes 2025-12-12
hide task count
short mode
```

---
## 🏋️ 오늘의 운동

### 🏊 수영 레슨
- 레슨 기록: 25 * 20

### 🏋️ 30분 루틴 (수영 후 보조 웨이트)
- [x] 머신 체스트프레스: ___kg × ___회 × 2세트 ✅ 2025-12-12
- [x] 레그익스텐션: ___kg × ___회 × 2세트 ✅ 2025-12-12
- [x] 시티드로우 머신: ___kg × ___회 × 2세트 ✅ 2025-12-12
- [x] 플랭크: ___초 × 2세트 ✅ 2025-12-12


---
## 📝 오늘의 기록

### 🌟 주요 활동
- 옵시디언 템플릿 작성 및 적용
- 자주 이기는 제안서 학습
### 💡 회고
- 잘한 일: 요동이와 데이트   

### 🤔 이슈 사항
- 재취업 시기 결정 


---
## 💭 메모